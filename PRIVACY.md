# 隐私声明（PRIVACY）

**当前版本（v3.6.0-r3）不做任何遥测。** 导入或运行本仓库代码不会向任何外部服务器发送版本号、心跳或使用数据；不存在任何默认开启的网络回传。

## 历史披露（如实）

自提交 `0efd980`（2026-08-10）起至 v3.6.0-r2 之间的版本，`lao/effect_anchored/__init__.py` 内含一条默认开启的版本遥测：导入包即在后台线程向 `https://effect-anchored-telemetry.zwiserfit.cn/ping` 发送版本号与一个本地生成的匿名 ID（可用环境变量 `EFFECT_ANCHORED_TELEMETRY=0` 关闭）。

关于这段历史，三件已实测核实的事实：

1. **该域名从未解析（NXDOMAIN）**——回传目标不可达，**没有任何遥测数据被实际回收**；
2. 该遥测可能在用户主目录创建过 `~/.effect_anchored_anon_id`（UUID v4）；
3. 自 v3.6.0-r3 起，该代码已整体移除。

## 自查与清理

检查本机是否残留匿名 ID 文件：

    ls ~/.effect_anchored_anon_id

如存在且希望删除：

    rm ~/.effect_anchored_anon_id

## 未来版本

任何安装/使用统计（若引入）必须满足：默认关闭、首次运行显式询问、字段白名单、可随时撤回，并事先在本文件声明。设计约束见下一版分发侧需求《安装漏斗追踪》。

---
最后更新：2026-08-26 · 随 v3.6.0-r3 发布
