# RELEASE NOTES

## v3.6.0-r3（2026-08-26）

- **隐私**：移除包导入时默认开启的静默遥测回传；历史情况与自查方式如实披露于 `PRIVACY.md`。本版导入与运行不产生任何对外网络回传。
- 修复官方账单 CSV 价目解析 bug（`time` 列不存在导致峰谷价目表恒为空）。
- `/v1/savings` 返回体新增 `disclaimer` 口径标注（仅覆盖重启后窗口、内存估算、非官方对账口径）。
- 事件日志停用 `saving_usd` 字段；对外金额口径一律为人民币 ¥。
- RIS（运行时可靠性层）为独立产品，不随本仓库发布。
